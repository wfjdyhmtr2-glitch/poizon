# ---------- 依赖与构建产物（不要提交）----------
app/node_modules/
app/out/
app/dist/
publish/
deploy/
screenshots/

# ---------- 系统 / 编辑器 ----------
.DS_Store
**/.DS_Store
*.log
.vscode/
.idea/

# ---------- Agent 工作目录（含本机笔记，仓库公开时绝不能入库）----------
.workbuddy/

# ---------- 只提交 app/.env.production ----------
# 该文件里只有 Supabase 的 URL 与 anon key（浏览器端公开密钥，安全性由数据库 RLS 保证）
# 本地覆盖文件不入库
app/.env.local
app/.env.*.local
